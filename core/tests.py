import datetime

from django.test import TestCase
from django.utils import timezone

# A test to make sure the index page is returning exactly 4 events, all in the future, in order starting with soonest.



# A test to make sure the events list is returning all future events in order starting with the soonest
